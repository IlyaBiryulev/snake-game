import { createSlice } from '@reduxjs/toolkit/react';

const initialState = {
  data: '',
};

export const gameReducer = createSlice({
  name: 'gameReducer',
  initialState,
  reducers: {
    //create reducers
  },
});
